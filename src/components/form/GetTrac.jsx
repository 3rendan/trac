import React from 'react'
import { tracReducer } from '../../context/TracsContext'

const GetTrac = () => {
  return (
    <>
      { trac.map((t) => {
        render
      })}
    </>
  )
}

export default GetTrac
